# TODO

- [ ] **T.128 app-sharing server** - NetMeeting application/desktop sharing (deferred: needs a real NetMeeting T.120 packet capture to validate the MCS choice-index width + channel model before the relay-fidelity fixes can be trusted; see DONE.md)
