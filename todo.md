# TODO

## Uncommitted work (finish before commit)

- [x] Finger: `ServiceFinger` on/off config flag added (`ConfigNames.cs`, default true in `HiveDbContext.cs`) and gated in `Mind.cs`
- [x] Finger: admin panel toggle - new Services section in `admin.hive.com/index.html` driven by `/api/servicetoggle` and `/api/status`
- [x] Finger: README entries - Service Ports row (79/TCP), feature section, Docker port, config toggle
- [x] Finger: tests for `BuildUserList` (empty, populated, skips blank screen names) added to `FingerTests.cs`
- [x] IA worker: documented the Edge Worker toggle + URL in the README Configuration section
- [x] `wayback-proxy-test/` scratch scaffold deleted

## Quick wins

- [x] NU1903 cleared - pinned `SQLitePCLRaw.lib.e_sqlite3` 3.53.3 (patched SQLite ≥ 3.50.2, CVE-2025-6965); `dotnet list package --vulnerable` is clean for both projects
- [x] Removed the `[PROXY-DEBUG]` stderr spam in `HttpProxy.cs` (and the matching `[LISTENER]` keep-alive spam in `Listener.cs`)
- [x] ICQ banner URL `192.168.69.1` -> `api.hive.com` (`OscarIcqService.cs`)
- [x] FTP proxy placeholder password gate removed (`FtpRequest.cs`); real FTP auth stays roadmap
- [x] Dockerfile installs ffmpeg; `FfmpegUtils` falls back to PATH ffmpeg when the bundled binary is absent; bundled ffmpeg binaries dropped from the Linux image (fixes arm64 + saves ~240MB)
- [x] README `docker run` example gains SOCKS `1996` and Finger `79`
- [x] `HiveController` download path traversal hardened - `file` param resolved with `Path.GetFullPath` and confirmed inside the repo root
- [x] CI: new `.github/workflows/ci.yml` builds + tests on push/PR (ubuntu + windows matrix)
- [x] `actions/setup-dotnet@v3` -> v4 in `ci-release.yml`

## Bigger gaps

- [x] Service toggles made functional - `Mind.Start()` gates each listener on its `Service*` flag (SMTP, POP3, IMAP, IRC, Usenet, DNS, Printer, ILS, RAS, H323, T120, Finger); intranet gated in `LocalServerProcessor` with `admin.hive.com` always exempt so the panel can re-enable it. Listener changes apply on restart, intranet applies immediately
- [x] README aligned - Contributing no longer claims debug builds enable extra services; Configuration documents the runtime toggles and restart semantics
- [x] `LocalServerFileProvider.GetDirectoryContents` returns `NotFoundDirectoryContents.Singleton` instead of throwing
- [x] FTP response caching dead block removed from `FtpProxy.cs` (documented why it's not implemented)
- [x] `tools/UsenetCurator/README.md` written - pipeline phases, modes, CLI flags, and how the output feeds `NntpProxy`
- [x] HTTPS proxy (9999) - working as designed. Serves the intranet plus DialNine, the live "outside line" (dial 9) that forwards old browsers to still-working external HTTPS services via the OpenSSL 1.0.2a stack. The SHA-NI crash was fixed in 223b307. No ProtoWeb/IA passthrough by design: archive reconstruction is HTTP-only; DialNine must reach the live service, never an archived copy.
- [ ] SOCKS5 auth, T.128 app-sharing server, Gopher, Yahoo!/MSN - out of scope this pass (new-protocol roadmap items)

## Cleanup

- [x] H.323 stack: the ~27 bare `catch {}` around `ReadExtensionAdditions` replaced with `PerDecoder.TryReadExtensionAdditions()`, which catches the specific decode exceptions and documents why the best-effort swallow is correct
- [x] `PrinterProxy` unknown IPP operations now return a well-formed `ServerErrorOperationNotSupported` response instead of throwing
- [x] `HttpProxy` request tracking now skips all `*.hive.com` intranet hosts (was a hardcoded `admin.com`/`hive.com` check)
- [x] `SCUtils.GetStationById` - removed the stale cache TODO (caching is already implemented directly below it)
- [x] `.editorconfig` expanded with the Allman / always-braces / no-line-limit style rules
- [x] Centralize the `hive.com` base domain - done. All subdomains, host checks, and email addresses derive from `HiveDomains.Base`; const concatenation (`"admin." + Base`) keeps it usable in `[Domain(...)]` attributes.
- [x] `RepoUtils` custom download repos - done. Repos are stored in config and managed from the Local Server admin page (add with validation, delete); the built-in downloads folder is reserved and can't be removed.

## Test coverage

- [x] `InternetArchiveProcessor` - `RewriteToWorker`, `GetArchiveTypeCode`, and `ProcessCDX` made internal and covered by `InternetArchiveTests.cs` (18 tests over the new Edge Worker path)
- [x] Finger `BuildUserList` covered (see above)
- [x] Protocol-flow coverage.
  - IRC: 62 tests. Pure logic (`IrcProtocolTests.cs`) plus RFC 1459/2812 command->response conformance through the real handlers (`IrcConformanceTests.cs`): registration 001-004, JOIN 331/353/366, error numerics; nick length asserted as an intentional deviation from RFC's 9-char limit, cited.
  - FTP: 28 tests (`FtpProtocolTests.cs`). RFC 959 reply codes, reply/command wire format (extracted pure `FtpRequest.ParseCommandLine`/`FormatResponse`/`FormatFeatureList`), and TYPE handling. The 430 auth-failure code is documented as an extension over RFC 959's 530.
  - Mail: 25 tests (`MailProtocolTests.cs`). SMTP (RFC 5321), POP3 (RFC 1939), IMAP (RFC 3501) command->reply conformance driven one-shot through the real handlers, DB-backed harness.
  - Telnet BBS: 14 tests (`TelnetBbsTests.cs`) - command registry + hidden filtering, window stack, the riddle game, and word wrap. Telnet here is raw ASCII (no RFC 854 negotiation), so these are application-logic tests. A test caught and fixed a real bug: winning the riddle on the first guess erased the "Correct! You win!" message (`UpdateText` re-fired the initial prompt because `attemptsLeft` was still 3).
  - Printer (LPD): 7 tests (`LpdProtocolTests.cs`). RFC 1179 control-file parsing (H/P/J/N fields, CRLF handling, short/empty lines). IPP wire conformance is left to SharpIpp; format detection is already covered by `PrintFormatDetectorTests`.
  - Streaming (PNA): 9 tests (`PnaProtocolTests.cs`). The RM->PNA 0x5a stream-packet framing, the RealPlayer client-hello parser, and RM FourCC decoding. Live transcoding is ffmpeg/socket-bound.
  - Streaming (MMS): 13 tests (`MmsProtocolTests.cs`). MS-MMSP TcpMessageHeader framing + Data-packet header, the message parser (round-trips build->parse), and the null-terminated UTF-16LE command-field string codec. The ASF session itself is ffmpeg-bound.

## Discovered during manual testing (pre-existing, NOT introduced here)

- [x] **Template cache collides across domains - FIXED.** Fluid's template cache was only disabled when a debugger was attached (`Mind.IsDebug`), so released/`dotnet run` builds cached parsed partials by bare path (`partials/header.html`). `hive.com` and `admin.hive.com` ship different files under those names, so whichever domain rendered a shared partial first won the slot process-wide - browse `http://hive.com/` then open the admin dashboard and it 500'd with `FileNotFoundException: partials/menu.html.liquid`; the reverse silently stripped hive.com's menu. Fox never saw it in dev because the debugger disabled the cache. Fixed by disabling `TemplateOptions.Default.TemplateCache` unconditionally in `LocalServerProcessor` (negligible cost at this traffic; every include now re-resolves through the per-request FileProvider). Verified live: hive.com->admin and admin->hive.com both render 200.
- [x] **Flaky RTP relay test - FIXED.** `RtpRelayManagerTests.GetStatistics_WithActiveRelays` intermittently threw `SocketException: Only one usage of each socket address` from `RtpRelay`'s bind. Root cause was a TOCTOU race in `RtpRelayManager.AllocateEvenPort`: it probe-bound a port, released it, then rounded to an even port and bound `evenPort`/`evenPort+1` - neither of which was reliably verified free - so the RTCP port (`evenPort+1`) could collide with a port another test held. Fixed by dropping the misleading test-binds and making the real bind authoritative: `CreateRelay` now retries with a fresh pair on `SocketException` (up to 10 attempts). Hardens the production NetMeeting relay too, not just the test. Verified: 25 iterations under heavy ephemeral-port contention + 45 full-suite runs, zero failures.
- [x] **Related - shared FileProvider mutated per request - FIXED.** `new TemplateContext(model)` uses the static `TemplateOptions.Default` (where the custom filters/`UnsafeMemberAccessStrategy` live), and `ControllerManager` used to assign `Context.Options.FileProvider` on it per request, which raced across concurrent cross-domain requests (one render could pick up another domain's FileProvider). Fixed by registering a single shared `LocalServerFileProvider` on `TemplateOptions.Default` once, and having it read the per-request controller root from an `AsyncLocal` (`SetCurrentRoot` called in `ControllerManager`) so each async request flow is isolated. Verified with a concurrency stress test: ~760 interleaved hive.com/admin.hive.com requests at up to 40-way concurrency, zero cross-domain contamination.

## Gotchas - full-codebase audit (2026-07-09)

_13-subsystem adversarial sweep. 107 findings confirmed after skeptic verification (19 high / 61 medium / 27 low); 24 refuted. `file:line` anchors are from the audit - re-confirm before editing. Grouped by severity for fix-first prioritization; subsystem tag in `()`. Several findings surfaced in two subsystems and were merged._

### ⚠ Contradicts already-checked items above (residual gaps in "done" work)

- [x] **Finger "Docker port" (line 5) is incomplete** - README `docker run` got `-p 79:79` but `Dockerfile` EXPOSE block (lines 9-72) never got `EXPOSE 79`. `docker run -P` / introspection won't publish Finger. **Fixed - `EXPOSE 79` added after the OSCAR entry.**
- [x] **"Service toggles made functional" (line 26) has two leaks** - `PrintSpooler.Init()` (`Mind.cs:179`) is NOT gated on `ServicePrinter`, so it polls printer.db at 10 Hz even with printing off; the SMTP postmaster thread is started unconditionally in `SmtpProxy`'s ctor and can never be stopped, polling PostOffice.db every 1s with SMTP off. Only the listeners got gated. **Both FIXED - `PrintSpooler.Init()` moved into the `ServicePrinter` `StartService` lambda; the postmaster thread moved out of `SmtpProxy`'s ctor into an idempotent `StartPostmaster()` called from the `ServiceSmtp` `StartService` lambda (same pattern, applies on restart). With SMTP off the PostOffice.db poll never starts; verified via harness: construction spawns no thread, double-start is a no-op, and the gated path still delivers queued mail to a user's INBOX end-to-end. Note: with SMTP toggled off, previously-queued undelivered mail sits until SMTP is re-enabled - delivery is SMTP-side by design.**
- [x] **Next tagged release will ship no binaries** - `ci-release.yml:45` bare `dotnet publish` now resolves to `VintageHive.sln`, whose `Release|x64` has no `Build.0` entries -> publish produces empty output (exit 0), packaging step then fails on a nonexistent dir. **Fixed - `ci-release.yml` now publishes `VintageHive.csproj` explicitly (same as the Dockerfile); verified locally with the exact CI command (win-x64 self-contained publish -> 214 files incl. VintageHive.exe). The sln still lacks `Release|x64` `Build.0` entries, so `ci.yml`'s `dotnet build` step remains a masked no-op - root-cause sln fix left open.**

### Fix-first (process crash, data loss, or a whole service broken)

- [x] (core) `UdpListener.Run` is `async void` with the UDP bind OUTSIDE the try - a port conflict throws synchronously out of the ThreadStart and kills the whole process (`Network/UdpListener.cs:95`). **Fixed - bind wrapped in try/catch mirroring `Listener.cs:80-91` (log, `_running=false`, set `_startedEvent`, return); `WaitForStart` now also requires `_running` so it returns false for a dead listener instead of true. `Run` stays `async void` (the loop awaits `ReceiveAsync` and every await site is inside catch-alls; the bind was the only synchronous escape). Verified with a bind-conflict harness: process survives, error logged, normal listen/echo path unaffected.**
- [x] (dns) DNS name-compression pointer followed with no loop/depth guard -> `StackOverflowException` (uncatchable) crashes the process (`Proxy/Dns/DnsProxy.cs:232`). **Fixed - `ParseDomainName` rewritten iteratively (no recursion), with three guards: pointers must jump strictly backward (`pointer >= current` rejected), targets must strictly decrease across follows (`pointer >= minPointer` rejected - kills revisit cycles that backward-only alone misses), and a 128-hop budget backstop; plus the RFC 1035 255-byte name cap. Verified via harness: self-pointer, two-pointer cycle, and a backward-only-legal revisit cycle all return null; forward pointers rejected; valid names and valid backward-pointer compression still parse with correct offset advance. End-to-end: a live `DnsProxy` fed the self-referential packet survived and still answered a subsequent valid query - proving the process no longer dies.**
- [x] (http/printer) `HttpRequest.Build` body-continuation loop: infinite 100%-CPU hot loop on client FIN/half-close, reads raw `NetworkStream` under TLS (corrupts multi-read HTTPS POST + TLS session), unbounded in-memory body accumulation, char/byte count mixing (`Proxy/Http/HttpRequest.cs:234`). **Fixed as ONE combined rewrite with the framing item below - see that entry. Body loop now breaks on read<=0 (kills the FIN spin), reads `SecureStream` under TLS, caps at 64 MiB, and accumulates bytes (decoded once at the end). Verified via plaintext harness: multi-chunk body length exact, multibyte-UTF-8 body across the segment boundary intact (no U+FFFD), FIN mid-body returns Invalid in <3s instead of spinning, oversize Content-Length rejected fast.**
- [x] (http) `SetExternal` forwards EVERY method as GET and `DefaultRequestHeaders.Add` throws on content headers -> all POSTs through DialNine/Helper fail; cache key `HPC-{uri}` ignores method so a POST can be answered from a cached GET (`Proxy/Http/HttpResponse.cs:306`). **Fixed - `SetExternal` builds an `HttpRequestMessage` with the original method + body (`ByteArrayContent`), forwards headers via `TryAddWithoutValidation` (routing content headers to `Content`, never throwing), and strips hop-by-hop headers (incl. Host). The `HttpProxy` cache key now includes the method (`HPC-{method}-{uri}`) so a POST can't be answered from a cached GET.**
- [x] (telnet) `async void` timer handler in news/gallery windows -> unhandled throw (or NRE on unparseable SmartReader fetch) crashes the process (`Proxy/Telnet/Commands/News/TelnetNewsArticleView.cs:53`). **Fixed - the whole `Startup_Timer` body is wrapped in try/catch (on failure it shows the "type [q]uit" message and logs, instead of crashing the process from a timer thread); a null article is handled; `GetImageLinks` uses `Uri.TryCreate` + `continue` for relative/empty scraped `<img>` src.**
- [x] (mail) SMTP session delivers only ONE message - 2nd `MAIL`/`DATA` throws duplicate-key `ArgumentException`, client hangs with no reply, queued mail lost; `RSET`-first and repeated `HELO` also throw (`Proxy/Smtp/SmtpProxy.cs:92`). **Fixed - crash-prone `bag.Add` sites (HELO/EHLO/MAIL/DATA/Username/Password/Authenticated) swapped to indexer assignment; MailFrom/MailTo/MailData cleared after delivery so message 2 can't inherit message 1's recipients; RSET guards the empty bag. The Authenticated/Username/Password swaps also fix the same crash on `AUTH LOGIN` retry after a failed login (same bug family, found while fixing). Verified via harness driving the real handler: two-message session delivers both with correct recipient isolation, repeated HELO / RSET-first / auth-retry all reply instead of throwing. Still open (adjacent, pre-existing): `DATA` without `MAIL FROM` hits `KeyNotFoundException` at delivery instead of a 503.**
- [x] (mail/core) `DateTime.Parse` on the client `Date` header (incl. named-timezone forms like `EST`) throws -> server accepts with 354 then silently drops the mail and never replies, poisoning the session for later `DATA` (`Data/Contexts/PostOfficeDbContext.cs:28`). **Fixed - `DateTime.TryParse` (invariant culture, so English month names parse regardless of server locale) with `DateTime.UtcNow` fallback; Subject/Date regexes now `IgnoreCase`. Verified via harness: lowercase `date:` header with an `EST` zone inserts instead of throwing, and an EST-dated message delivered through the live SMTP path lands in the DB.**
- [x] (oscar) Static `Sessions` List is unsynchronized and enumerated while mutated; `GetByScreenName` NREs on pre-auth (null `ScreenName`) sessions - one stalled AIM-MD5-login or half-open TCP connect to 5190 breaks offline-buddy lookups + ICBM delivery server-wide until that socket closes (`Proxy/Oscar/OscarServer.cs:13`). **Fixed - `Sessions` is now a `ConcurrentDictionary<ulong, OscarSession>` keyed by session id; the `GetByScreenName` extension null-guards the argument and skips sessions whose `ScreenName` is null (pre-auth), so a stalled login no longer NREs every lookup. All 8 call sites work unchanged (the extension now targets the dictionary). Verified via harness + MSTest: a null-ScreenName session in the registry doesn't break lookups.**
- [x] (oscar) Session leaks forever on early client abort (hello FLAP + `Sessions.Add` are outside the try); graceful FIN hot-spins a pool thread at ~1000/s; ghost session stays "online" to buddies (`Proxy/Oscar/OscarServer.cs:79`). **Fixed - `Sessions[id] = session` and the hello FLAP are now inside the try, the departure/`TryRemove` run in a `finally` (so an early hello throw no longer leaks a ghost session), and the receive loop `break`s when `ReceiveFlaps` returns null (a disconnect) instead of `Thread.Sleep(1); continue;` - killing the ~1000/s hot-spin.**
- [x] (oscar) FLAP framing assumes one `ReadAsync` returns a whole frame - any frame split across reads or >4096 bytes (SSI bulk adds, BART icon uploads, large ICBMs) deterministically disconnects legit clients (`Proxy/Oscar/OscarSession.cs:202`). **Fixed - `ReceiveFlaps` now reads the 6-byte FLAP header exactly (via a `ReadExactAsync` helper), then reads the exact declared payload length into a single frame before decoding; `read==0` mid-frame returns null (disconnect), and a bad marker is treated as a framing desync. Verified via harness + MSTest: a frame split across two reads (straddling the header) is reassembled; the old code threw on the partial.**
- [x] (printer) IPP request body is round-tripped through `Encoding.ASCII` -> every byte ≥0x80 becomes `?`, corrupting all binary PDF/PostScript document data before storage (`Proxy/Printer/PrinterProxy.cs:50`). **Fixed - `HttpRequest.Parse` now slices `BodyData` from the exact original byte buffer at the byte offset of the header terminator (`FindHeaderTerminator`), instead of `encoding.GetBytes(rawBody)`. Binary bodies survive under any encoding. Verified: a POST body of `00 FF 80 41 E9 25 50` round-trips byte-identical (was `?`-mangled). Fixes the follow-up flagged during the framing rewrite.**
- [x] (printer) ESC/P page bomb: each `FF`/`LF`/`ESC J` allocates an uncapped ~48 MB page image with no page-count cap -> an unauth LPD/IPP/raw client can OOM the whole process (`Proxy/Printer/EscPosRenderer.cs:154`). **Fixed - a `MAX_PAGES` cap (200): `NewPage` stops allocating past the cap and sets `_pageCapReached`, and the `Process` loop exits when it's set (truncating the render with a WARN). An unauth stream of form-feeds can no longer OOM the process.**
- [x] (printer) IPP `Create-Job`/`Send-Document`/`Print-URI` discard the document and strand jobs in state `New` forever (breaks CUPS / classic OS X / Linux clients); `Purge-Jobs` is a no-op so they can't even be cleaned via IPP (`Proxy/Printer/PrinterProxy.cs:210`). **Fixed - `Send-Document` now attaches the document bytes to the Create-Job job (by `JobId`) via `SetJobDocumentData` and moves it to Pending for the spooler (was creating a fresh job and discarding the doc); `Print-URI`/`Send-URI` return `ServerErrorOperationNotSupported` without stranding a job (VintageHive doesn't fetch print URIs); `Purge-Jobs` calls the new `PurgeTerminalJobs()` DELETE. Verified via harness: purge removes a completed job.**
- [x] (tls) `SslStream.ReadAsync` treats a TLS record split across TCP segments (or two records in one read) as a disconnect, its error check is inverted (never fires), and >4096 decrypted bytes strand pending plaintext (`Proxy/Security/SslStream.cs:102`). Loop `SSL_read` on `SSL_ERROR_WANT_READ`, drain pending before another network read, throw on `SSL_ERROR_SSL/SYSCALL`, honor the caller's buffer length.
- [x] (tls) Native SSL handle (SSL + both memory BIOs) leaks on nearly EVERY HTTPS connection - the custom `SslStream` has no finalizer and its only `SSL_free` site (`Listener.cs:225`) is unreachable on the normal success path; retro browsers open one connection per resource, so it bleeds native memory over days (`Proxy/Security/SslStream.cs:56`). **Fixed - the per-connection `Listener.Run` lambda is now wrapped in try/catch/finally, and the `finally` is the single deterministic teardown: `sslStream?.Dispose()` (SSL_free releases the SSL handle + both owned BIOs), `networkStream.Dispose()`, `connection.Close()`. `SslStream.Dispose` is now idempotent (zeroes `Handle`, `SuppressFinalize`) with a finalizer backstop for any path that skips Dispose. Verified: 25 sequential connections + 100× cert round-trips clean, full HTTP path intact.**
- [x] (streaming) The global session-create semaphore is held across an unbounded ffmpeg header read - one silently-stalled WMA/MMSH station bricks ALL WMP/MMS radio session creation (RealAudio bricks independently via the same pattern) (`Processors/LocalServer/Streaming/RadioMmshStreaming.cs:1104`). Timeout the header phase, kill+dispose ffmpeg/httpClient/upstream on failure inside the lock, use a per-stationId lock. **Fixed - the single global `_sessionCreateLock` is replaced by per-station locks (`GetSessionCreateLock` over a `ConcurrentDictionary<string, SemaphoreSlim>`), so a station whose header read stalls only blocks itself. The three ASF-header `ReadExactAsync` calls now take a `CancellationToken` bounded by a 20s `headerCts`. `httpClient`/`upstreamResponse`/`ffmpeg` are hoisted and, on any failure (timeout, upstream/ffmpeg fault), a `catch` kills the ffmpeg tree and disposes all three inside the lock before rethrowing - previously a header-read throw leaked every one of them. Ownership nulls the locals once `MmshLiveSession` is constructed so the catch can't double-dispose.**
- [x] (docker) `VOLUME` + README mount point are `/app/data` but the app persists everything under `/app/vfs` - all user data (mail, printer jobs, config, radio) is lost on container recreation (`Dockerfile:75`). **Fixed - `VOLUME /app/vfs` declared in the final stage AFTER the mkdir+chown (declaring it in the base stage would snapshot the path before ownership is set); `/app/vfs` pre-created in the useradd RUN; README mount updated to `/app/vfs`. No migration concern: nothing ever wrote to `/app/data`. Not yet validated with a `docker build` (daemon was down).**
- [x] (startup) `Mind.Init` hard-depends on RadioBrowser + GeoIP fetches with no error handling - an offline or degraded-internet start crashes the process before any service comes up (`Utilities/RadioBrowserClient.cs:109`). **Fixed (core) - the three network-dependent startup calls (`RadioBrowser.Init`, `GeoIpUtils.CheckGeoIp`, `ProtoWebUtils.GetSites`) are each wrapped in try/catch that logs a WARN and continues, so an offline start now brings all services up with those features degraded instead of crashing before any listener binds. The deeper robustness the audit suggested (lazy RadioBrowser with a fallback host, null-checked tags fetch, internal `CheckGeoIp` catch) is left as optional follow-up - the crash-on-start, which was the headline, is resolved.**

### Medium - reachable, bounded (degradation, leaks, protocol correctness)

**HTTP / archive:**
- [x] `SetExternal` lowercases the ENTIRE outbound URL incl. path+query; both real callers hit it (`Proxy/Http/HttpResponse.cs:297`). **Fixed - dropped the `ToLower()`; `Uri.ToString()` already lowercases scheme+host, and no cache key or test depended on the lowercased form. Verified on the wire: `SetExternal` against a local server now requests `/MixedCase/Path?Query=UpPeR` verbatim.**
- [x] UA-less requests throw `KeyNotFoundException` at every `Headers[UserAgent]` site incl. the error-response path -> client gets no response at all (`Proxy/Http/HttpProxy.cs:85/176/206`). **Fixed - `HttpRequest.UserAgent` now uses `TryGetValue` (the `?? "NA"` was dead since the dict indexer throws), and all three `HttpProxy` `RequestsTrack` sites route through the property. Verified: UA-less request returns "NA" instead of throwing, present UA passes through, `Invalid.UserAgent` no longer throws.**
- [x] `HttpRequest.Parse` crashes on colon-less header lines, `=`-less cookies, and HTTP/1.0 requests with no `Host` -> client hangs, no 400 (`Proxy/Http/HttpRequest.cs:84`). **Fixed - malformed header lines skipped, valueless cookie fragments skipped, duplicate cookie names last-wins, and the Uri is built via `Uri.TryCreate(Absolute)` + an http/https scheme guard (required - `TryCreate` succeeds as `file:///` for a no-Host origin-form request on Linux) returning `Invalid` instead of throwing. Also plugged the two now-reachable NRE landmines the audit flagged: `PrinterProxy` dereferenced `httpRequest.Uri` before its `IsValid` check (moved after), and `Listener`'s CONNECT-preamble `Parse` had no `IsValid` guard before `baseRequest.Uri.Host` (added, closes the socket). Verified via harness driving real `Parse`: colon-less header, bare cookie, dup cookie, no-Host, valid request, and CONNECT preamble all behave.**
- [x] Negative Wayback-availability cache throws (null `SqliteParameter`) -> 500 instead of graceful miss, and never caches the miss; `int.Parse('-')` on the CDX length column (`Processors/InternetArchiveProcessor.cs:414`). **Fixed - `ProcessCDX` parses the length with `int.TryParse` (0 fallback for the `-` placeholder); `GetAvailabilityInternal` now persists the outcome unconditionally (moved out of the non-empty-body `if`, with an `else` for the empty-body case) using empty string as the negative sentinel, so a dead URL is a `[CacheHit]` next time instead of re-hitting CDX/the worker. Defensively, `CacheDbContext.SetWaybackAvailability` binds `(object)value ?? DBNull.Value` and `GetWaybackAvailability` reads `IsDBNull(0) ? "" : GetString(0)` so a null can't crash either side. Verified via harness: `-` length -> 0, null/empty/value round-trip through the real cache, absent row still reads null.**
- [x] Swallowed stream-copy failures leak the fresh-per-request `HttpClient` upstream socket and hide all response-path write errors at DEBUG (`Proxy/Http/HttpProxy.cs:171`). **Fixed - the response-write `try` now has a `finally` that disposes `httpResponse.Stream` on every path (success, disconnect, fault); the success-only dispose was removed. The single swallow-all catch is split: routine `IOException`/`SocketException`/`ObjectDisposedException` disconnects stay at DEBUG, everything else logs at WARN. Never touches `socket.Stream`/`SecureStream` (Listener owns those); the guarded `try { … } catch { }` dispose tolerates the radio paths that already disposed via `using`. Verified via harness driving real `ProcessRequest` over sockets: the content stream is disposed after a clean copy AND when the copy throws (the leak), with the fault now surfacing at WARN.**
- [x] Archived HTML re-encoded with `Encoding.ASCII` -> all non-ASCII bytes become `?`, cached 365 days; the `"utf8"` charset check is misspelled (should be `utf-8`) so the UTF-8 branch is dead (`Processors/InternetArchiveProcessor.cs:167`). **Fixed - new `GetHtmlEncoding(contentType)` helper (UTF-8 when the header says utf-8/utf8, case-insensitive; otherwise byte-bijective ISO-8859-1). Both the worker and direct HTML paths now decode from bytes (`ReadAsByteArrayAsync`) and re-encode with that encoding instead of `Encoding.ASCII`, so Latin-1/CP-1252 and UTF-8 pages survive intact. The two dead `Contains("utf8")` charset blocks collapsed to `res.SetEncoding(GetHtmlEncoding(contentType))`; `CacheKeyVersion` bumped v2->v3 to flush the `?`-poisoned entries. Verified via harness driving the real `ScrubHtml`: `0xE9/0xA9/0xAE` (Latin-1) and `€` (UTF-8) round-trip with no `0x3F`. Also removed the dead `Debugger.Break` scaffolding in the direct path.**
- [x] ProtoWeb fetches every uncached page TWICE and pairs headers from fetch #1 with the body of fetch #2 (`Processors/ProtoWebProcessor.cs:53`). **Fixed - single `using var proxyRes = await proxyClient.GetAsync(...)`, `EnsureSuccessStatusCode()` (throws on non-2xx exactly as the old `GetByteArrayAsync` did, so the `catch`/fall-through is preserved), then `ReadAsByteArrayAsync()` from that same response. Headers and body now come from one transaction; halves load on the volunteer ProtoWeb proxy and disposes the response message.**
- [x] A single transient ProtoWeb outage permanently flips `ServiceProtoWeb=false` in config with no recovery path; the null sitelist is negatively cached 6h and static site lists never refresh (`Utilities/ProtoWebUtils.cs:45`). **Fixed, all three legs. (1) The outage catch no longer writes `ServiceProtoWeb=false`; it sets a runtime `ProtoWebUtils.IsOnline=false` + `offlineSinceUtc`, with a 5-minute cooldown short-circuit at the top of `GetSites` (so an outage doesn't stall every request on the 10s timeout) that self-heals - `IsOnline=true` on the next successful fetch. (2) `CacheDbContext.Do` no longer persists a null result (it was serialized to the literal `"null"` and cached for the full TTL); audited all `Do` callers - only `RadioBrowserClient.StationGetAsync`'s unknown-station lookup loses its 10-min negative cache, which is harmless. (3) `ProtoWebProcessor`'s static HTTP/FTP site lists now reload after 6h (a reload within the window is a SQLite cache hit, no network) instead of populating once forever. Verified via harness: null not cached (re-runs), non-null cached, offline path returns in <1s with `ServiceProtoWeb` still true. **Users whose config was already flipped false by the old bug stay off until they toggle it back on - the fix does not retroactively re-enable a now-genuine preference.****
- [x] **NOTE (from the ProtoWeb work):** `/api/status`'s `protoweb` field reflects only the config toggle, so during an outage it now stays `true` while `ProtoWebUtils.IsOnline` is `false`. Optional polish: surface `IsOnline` as a separate health field in the admin status payload. **Done - `/api/status` now emits `protowebOnline = ProtoWebUtils.IsOnline` alongside the `protoweb` config toggle, so the dashboard can distinguish "enabled" from "actually reachable" during an outage.**
- [x] One TCP read is treated as one complete request - headers split across TCP segments are dropped/misparsed on slow links; HTTPS POST bodies spanning reads read the raw `NetworkStream` -> TLS corruption (`Network/Listener.cs:205`, `HttpRequest.cs:237`). **Fixed (combined with the body-loop item above) - `HttpRequest.Build` rewritten to accumulate bytes in a `MemoryStream`: Phase 1 reads until the `\r\n\r\n` (or `\n\n`) header terminator via a new `ReadMoreAsync` that branches to `SecureStream` under TLS; Phase 2 reads the declared body the same way. Non-HTTP garbage is rejected up front by a verb-prefix check; header/body caps (64 KiB / 64 MiB) bound memory; the whole request is decoded exactly once. Companion fix REQUIRED for the TLS half: `Proxy/Security/SslStream.cs` `ReadAsync` was rewritten to an SSL_read-first loop that drains buffered plaintext, feeds ciphertext on `WANT_READ` (so a TLS record split across TCP segments is reassembled instead of read as a disconnect), honors the caller's buffer length (was a hard 4096-copy -> overflow/garbage), and returns 0 (never -1) on EOF/fatal so the Listener's `read <= 0` teardown fires cleanly. Verified: plaintext framing (split headers, multi-chunk + multibyte body, FIN, LF-only, cap, garbage) end-to-end, 8/8. **TLS caveat: a modern Windows Schannel client cannot complete a handshake against the retro OpenSSL 1.0.2a server (fails in `AuthenticateAsServer`, which this change does NOT touch), so the secure-read path is verified by inspection + shared Build logic rather than end-to-end here - exercise it with a real period browser or the live DialNine HTTPS path.****
- [x] **NEW follow-up (surfaced by the Build rewrite):** `HttpRequest.Parse` ignores its `rawData` byte[] param and sets `BodyData = encoding.GetBytes(rawBody)` - round-tripping the body through a string. Under `Encoding.ASCII` (PrinterProxy/IPP) every byte >0x7F becomes `?`, so binary PDF/PostScript print jobs are still corrupted at Parse time even though Build now delivers the correct bytes. Fix: slice `BodyData` from the original `rawData` at the byte offset of the header terminator. (Was called out in the audit's printer items too.) **Fixed - `Parse` now computes `bodyBytes = rawData[FindHeaderTerminator(rawData)..]` and sets `BodyData = bodyBytes`, slicing the body verbatim from the original bytes instead of `encoding.GetBytes(rawBody)`; `Build` already hands `Parse` the full byte array. `PrinterProxy` consumes `httpRequest.BodyData`, so binary IPP/PDF/PostScript jobs are preserved byte-for-byte. Added `HttpRequestTests.Parse_BinaryBody_PreservedByteForByte` (a body with 0x80/0xFF/0x00 under ASCII survives intact); 12/12 HttpRequest tests pass.**

**Core / DB:**
- [x] No TTL sweep anywhere - cache.db, logs, requests, websession all grow monotonically; the `websession.ttl` column is written as the insert timestamp and never read; `vintagehive.log` never rotates (`Data/DbContextBase.cs:138`). **Fixed - `DbContextBase` gained `DeleteExpiredByTtl`/`DeleteOlderThan`/`Checkpoint`; `CacheDbContext.RunMaintenance` sweeps all 8 TTL cache tables (`DELETE WHERE ttl <= now`); `HiveDbContext.RunMaintenance` prunes logs >7d, requests >30d, and websessions >7d (the `ttl` column is now actually read - see the WebSession item). A `Mind.MaintenanceLoop` runs both hourly and checkpoints the WAL. Verified: expired cache rows dropped, fresh kept, both sweeps run clean against the real tables. (`vintagehive.log` file rotation itself is separate from the DB sweep - left as a follow-up.)**
- [x] Listener per-connection `Task.Run` lacks try/catch/finally on setup AND teardown -> HTTPS connection-setup failures (bad probes, failed handshakes, cert errors) vanish with no log line, and streams/socket/native-SSL aren't deterministically disposed on any path (`Network/Listener.cs:117/225/264`). **Fixed together with the native-SSL-leak item - the whole lambda is wrapped in try/catch/finally: setup failures now log via `Log.WriteException` instead of vanishing out of the async void, and teardown (SSL/stream/socket) is centralized in the `finally`. Also removed the leftover `[LISTENER]` `Console.Error` keep-alive spam that had regressed into the loop, and simplified the per-request response/teardown branches.**

**Mail:**
- [x] Unauth SMTP `DATA` accepts an unbounded in-memory payload, then crashes on missing `MAIL FROM` (no 503) - LAN memory-exhaustion vector, live on the default `0.0.0.0` bind (`Proxy/Smtp/SmtpProxy.cs:153`). **Fixed - `DATA` returns 503 unless both `MAIL FROM` and `RCPT TO` are present (was NREing at delivery on the missing `MAIL FROM`); the accumulated message is capped at 32 MB and returns 552 past it. Test updated: `Data_Replies354` now sets up a transaction first, and a new `Data_WithoutTransaction_Replies503` covers the guard.**
- [x] POP3 `DELE` and IMAP `EXPUNGE` each remove only half of the dual store (`emails` vs `message_mailbox`) -> permanent desync, phantom counts, deleted mail resurrecting, orphan growth; the declared FK is never enforced because `foreign_keys` pragma is off (`Data/Contexts/PostOfficeDbContext.cs:132`). **Fixed - `DeleteEmailById` now also deletes the `message_mailbox` links (so IMAP stops showing a phantom), and `ExpungeDeleted` cascades to the underlying `emails` row once no other mailbox references it (so POP3, which reads `emails` directly, stops seeing it and orphans stop accumulating). Manual cascade avoids a schema migration for the missing `ON DELETE CASCADE`.**
- [x] POP3 `int.Parse` on client args (and TCP-coalesced commands) crashes the handler -> client hangs instead of `-ERR` (`Proxy/Pop3/Pop3Proxy.cs:131`). **Fixed - all four raw `int.Parse` sites (LIST/RETR/DELE/UIDL) folded into their bounds checks with `int.TryParse`, matching the pattern TOP already used. Verified via harness: `RETR abc`, `LIST abc`, `UIDL abc`, and an overflowing `DELE` all reply `-ERR`; numeric args still work. TCP-coalesced command framing is the separate one-read-one-command item below.**
- [x] IMAP sequence-set parsing: `FormatException` on non-numeric ranges and a CPU-pinning loop on huge/overflow ranges (`Proxy/Imap/ImapProxy.cs:956`). **Fixed - `ResolveSequenceSet` range endpoints use `int.TryParse` (a non-numeric range is skipped, no more `FormatException`), and the non-UID expansion loop is clamped to `[1, messages.Count]` before iterating, so `1:4294967295` can't pin the CPU.**
- [x] IMAP/POP3/SMTP assume one TCP read = one command line - pipelined commands get absorbed/misparsed and their tags never answered (client stalls); IRC already does per-read line framing to copy (`Proxy/Imap/ImapProxy.cs:36`). **Fixed - all three (POP3, IMAP, SMTP) now buffer to CRLF in the connection DataBag, process every complete command line, and retain the trailing partial line (bounded) for the next read; the per-command logic moved to `ProcessCommandLine`/`ProcessRequestInner`. SMTP passes DATA-mode payloads straight through (no line-splitting of the message body) and hands the buffer remainder to the DATA handler if a `DATA` command appears mid-packet. Verified via harness: POP3 pipelined commands all answered, split command reassembled; full SMTP DATA delivery flow intact.**
- [x] IMAP `APPEND` returns `OK` without storing anything -> Sent-mail server copies silently vanish; literal data desyncs the session (`Proxy/Imap/ImapProxy.cs:461`). **Fixed (reply NO) - `HandleAppend` now replies `NO APPEND not supported` at the command line, before the synchronizing-literal continuation, so a compliant client never sends the data and keeps its local copy instead of losing it to a silent OK. Full literal-continuation storage remains a feature TODO.**

**IRC / Usenet / Telnet:**
- [x] Telnet session loop has no exception handling -> a throw leaks the session in a non-thread-safe static list and never closes the socket (permanent OS handle leak, remotely triggerable via connect+reset) (`Proxy/Telnet/TelnetServer.cs:29`). **Fixed - session registry is now a `ConcurrentDictionary` keyed by the per-connection trace id, and the session loop is wrapped in try/finally that removes the session and calls `Destroy()`. The socket itself is now closed by the Listener's own finally (the disposal-cluster fix). Discovered adjacent bug: `TelnetSession.ID = new Guid()` is always `Guid.Empty` (should be `Guid.NewGuid()`) - sidestepped by keying on `TraceId`; logged as a follow-up.**
- [x] `TelnetSession.Destroy` is never called on disconnect -> `TelnetCountCommand` window timers run forever (`Proxy/Telnet/TelnetSession.cs:143`). **Fixed - `session.Destroy()` is now called in the `TelnetServer` loop's `finally` (previously it wasn't called on ANY path, even a clean disconnect), tearing down the window manager and its per-window timers.**
- [x] IRC channel/user state uses plain `HashSet`s mutated (and read) concurrently across connections - worst case a corrupted bucket chain pins a pool thread (`Proxy/Irc/IrcChannel.cs:30`). **Fixed - new `ConcurrentHashSet<T>` (backed by `ConcurrentDictionary`); `IrcChannel.Operators/Voiced/BanMasks/InviteList/Modes` and `IrcUser.Channels/Modes` now use it. All existing usages (Add/Remove/Contains/Clear/Count/LINQ/collection-initializer) work unchanged; `Add` is now `TryAdd` so duplicate bans are naturally skipped.**
- [x] IRC broadcasts write another connection's `NetworkStream` concurrently with that connection's own reply writes -> interleaved/corrupted framing (esp. Linux/Docker partial sends) (`Proxy/Irc/IrcUser.cs:46`). **Fixed - `IrcUser.SendData` takes a per-user `SemaphoreSlim` around the socket write, serializing a broadcast against the user's own reply.**
- [x] IRC nick registration is check-then-act; plain message interleaving (not a tight race) lets two connections register the same nick because `CheckIfRegistrationComplete` never re-checks in-memory uniqueness (`Proxy/Irc/IrcProxy.cs:399`). **Fixed - a `ReservedNicks` `ConcurrentDictionary` is claimed with `TryAdd` at both registration (`HandleNickRegistration`) and rename (`HandleNickChange`), replacing the racy `Users.Values.Any` scans; the reservation is released on nick change and on disconnect (including nicks reserved during an incomplete registration).**
- [x] NNTP `DotStuff` misses dot-stuffing on the FIRST body line; 28 bundled articles start with `.`, and `alt.something #1058` (body is exactly `.`) prematurely terminates the multiline response and desyncs the session (`Proxy/Usenet/NntpProxy.cs:533`). **Fixed - leading-dot check added ahead of the CRLF-anchored Replace; verified: `"."` -> `".."`, leading/interior dots stuffed, plain bodies untouched. The Subject/From/Date CR/LF/TAB strip (defense-in-depth) was NOT done - left open.**
- [x] NNTP zero-result/failed archive lookups are never cached and the fetch discards its result (`Proxy/Usenet/UsenetDataSource.cs:53`). **Fixed - a miss (empty article list) is now cached with a short 15-minute TTL (hits keep the 24h TTL), so a repeated request for a dead/unknown group doesn't re-fetch from the archives every time.**
- [x] NNTP + IRC assume one command per TCP read - an IRC paste/burst spanning the 4096-byte buffer yields truncated/garbage commands with no carry-over (`Proxy/Usenet/NntpProxy.cs:556`, `IrcProxy.cs:132`). **Fixed - both NNTP and IRC now use the same per-connection CRLF accumulator (buffer, process complete lines, retain the bounded remainder). NNTP's POST is not-allowed (no multi-line body mode) so the accumulator is safe there.**

**NetMeeting:**
- [x] RAS registry grows unbounded on default-on UDP 1719 - a new endpoint id per RRQ (no dedup on re-register) plus an unclamped extension TTL (`Proxy/NetMeeting/H225/RasServer.cs:84`). Dedup by call-signal address, clamp TTL to a server max, cap registry size. **Fixed - `RasRegistry.FindByCallSignalAddress` lets a re-registration (keep-alive RRQ) from the same primary call-signal address reuse its existing endpoint id instead of minting a fresh entry each time; TTL is clamped `Math.Min(requested>0?requested:300, MAX_TTL_SECONDS=3600)`; `RasRegistry.Register` returns false once `MaxEndpoints=1024` is reached (new endpoints only) -> `EncodeRegistrationReject(RRJ_UNDEFINED)`. An expired-sweep timer already reaps stale entries.**
- [x] `H323Server._activeCalls` leaks entries on non-Released teardown paths, incl. a `SendReleaseComplete` throw reachable by an unauth `Setup` + RST (`Proxy/NetMeeting/H225/H323Server.cs:170`). `TryRemove(call.CallId)` in a finally regardless of state. **Fixed - `call` is hoisted to before the `try` and the `finally` now does `if (call != null) _activeCalls.TryRemove(call.CallId, out _)` unconditionally, replacing the old "sweep only calls already in `Released` state" logic. A throw before the call reached `Released` (SendReleaseComplete write failure on an unauth Setup+RST, a mid-processing exception) no longer strands the entry.**

**OSCAR:**
- [x] Concurrent unsynchronized writes to a session's `NetworkStream` (broadcasts vs. own replies) + non-atomic `SequenceNumber++` -> duplicate/non-monotonic FLAP sequence numbers, which clients treat as fatal framing errors (`Proxy/Oscar/OscarSession.cs:188`). **Fixed - `SendSnac`/`SendFlap` now take a per-session `SemaphoreSlim` covering the `SequenceNumber++` assignment AND the socket write, so a broadcast from another session can't interleave with the session's own reply. Verified via harness + MSTest: 200 concurrent sends produce 200 frames with 200 unique sequence numbers and intact framing.**
- [x] Privacy lists (`PermitList`/`DenyList`/`PrivacyMode`) are populated but enforced nowhere - ICBM, typing, warnings, presence all ignore them; the one-shot fake "offline" on block is contradicted by the next presence broadcast (`Proxy/Oscar/Services/OscarIcbmService.cs:78`). Central `CanSee(sender, recipient)` at ICBM/MTN/location/both presence paths. **Fixed - added `OscarSession.IsVisibleTo(otherScreenName)`, a single predicate over `PrivacyMode` (1 allow-all, 2 deny-all, 3 permit-only, 4 deny-list, 5 buddy-only; unknown -> open) that governs both directions (recipient permits sender for delivery; subject permits watcher for presence). Gated every leak path: ICBM send (blocked -> "not logged on" 0x04, so the blocker appears offline), MTN typing (silent drop), warnings, location get-info (blocked -> not-found, hiding profile + away message), status-change presence (`BroadcastStatusToWatchers`), and sign-on presence (single choke point in `SendUserOnline`, covering both announce directions). Because the same predicate drives receive AND presence, a denied party now both fails to receive and consistently sees the subject offline - no more one-shot fake-offline contradicted by the next broadcast. Verified: privacy-mode1..5 + unknown-mode + case-insensitivity harness checks pass.**
- [x] Chat cookie lifecycle: single-entry `PendingChatCookies` races concurrent joiners, never expires; unknown cookie NREs and silently drops the connection with no auth-failed FLAP; dead code at `:127` (`Proxy/Oscar/OscarServer.cs:122`). Per-joiner one-shot cookie map, TTL/room-removal purge, null-guard + auth-failed reply. **Fixed - `PendingChatCookies` is now keyed by a UNIQUE per-joiner cookie (`CHAT:{roomCookie}:{guid}`) via a new `OscarServer.IssueChatCookie(room)`, so two clients joining the same room no longer share one entry that the first redemption deletes. Each entry is a `PendingChatCookie(Room, ExpiresAt)` with a 5-minute lifetime; `PurgeExpiredChatCookies()` runs on `IssueChatCookie` and on every sign-on, so an issued-but-unredeemed cookie can't linger. The redemption path null-guards the auth-cookie TLV (a single-TLV sign-on that isn't a cookie now replies `AuthFailedError` instead of NRE-ing) and `ProcessCookieAuth` replies `AuthFailedError` on an unknown stored cookie rather than dereferencing a null `storedSession`. Removed the dead `originalSession` lookup and the redundant `cookieValue[5..]` room re-derivation (the removed cookie's `Room` is authoritative). Verified: chatcookie unique/registered/purges-expired/one-shot harness checks pass.**
- [x] Chat-invite accept path (`CLI_SERVICE_REQ` for family 0x0E) is unimplemented -> channel-2 chat invitations dead-end (`Proxy/Oscar/Services/OscarGenericServiceControls.cs:62`). Handle `SNAC(01,04)` -> `(01,05)` redirect reusing the `HandleCreateRoom` TLVs. **Fixed - `CLI_SERVICExREQ` now recognises a request for the chat service (0x0E): it parses the target room from TLV 0x01 (bounds-checked `TryParseChatRoomInfo` reading exchange + name, the same room-info layout `OscarChatRoom.EncodeChatRoomInfo` emits), resolves it via `GetOrCreateChatRoom`, issues a one-shot join cookie (`IssueChatCookie`), and replies with the `SNAC(01,05)` service redirect (TLVs 0x0D family, 0x05 `ip:5190`, 0x06 cookie, 0x01 room info) - the same redirect shape the chat-nav create path sends. So accepting a channel-2 chat invite now opens a chat connection and joins instead of dead-ending. Non-chat service requests keep the prior "not implemented" log. Verified: roominfo wellformed/overlong-rejected/too-short harness checks pass.**

**Printer:**
- [x] No intake size limits: LPD allocates the client-declared byte count and raw-9100 buffers an unbounded stream -> unauth LAN memory-exhaustion (`Proxy/Printer/LpdProxy.cs:204`). **Fixed - LPD control/data files with a declared size ≤0 or >64 MB are rejected with NAK before any allocation; `RawPrintProxy` stops accumulating past 64 MB with a WARN.**
- [x] Spooler dies silently on any DB exception (`GetNextJob` is outside the try, the catch can rethrow), the discarded `Task.Run` is unobserved, `Thread.Sleep(100)` blocks a pool thread at 10 Hz, and it polls even when `ServicePrinter` is off (`Proxy/Printer/PrintSpooler.cs:42`). **Fixed - the whole loop body (including `GetNextJob`) is wrapped in try/catch with a guarded best-effort abort; `Thread.Sleep(100)` -> `await Task.Delay(100)`; the `Task.Run` now has an `OnlyOnFaulted` continuation that logs; `Init` was already gated on `ServicePrinter` (earlier commit).**
- [x] Static `PrinterUrl` mutated per request from the client `Host` header -> races across concurrent IPP connections (affects `PrinterUriSupported`/`PrinterMoreInfo` + Get-Jobs/Get-Job-Attributes JobUris) (`Proxy/Printer/PrinterProxy.cs:52`). **Fixed - the static field is gone; `printerUrl` is a per-request local threaded into `GetPrinterAttributesResponse`/`GetJobsResponse`/`GetJobAttributesResponse`/`GetSendDocumentResponse`.**
- [x] Jobs wedge in `Processing` forever after a restart/crash mid-conversion - no startup requeue, `GetNextJob` only fetches `Pending`, no external `Processing`->anything transition; IPP then reports a permanently busy printer (`Data/Contexts/PrinterDbContext.cs:69`). **Fixed - `PrinterDbContext.RequeueProcessingJobs()` resets `Processing`->`Pending`, called from `PrintSpooler.Init`. Verified via harness: a Processing job is requeued and `GetNextJob` then returns it.**
- [x] Job table grows unbounded - doc/print blobs never cleared (`printData` is write-only dead weight, never read back), `Purge-Jobs` and LPD remove are silent no-ops (`Data/Contexts/PrinterDbContext.cs:372`). **Fixed - `SetJobState` nulls `docData`/`printData` in the state UPDATE on terminal states (Completed/Canceled/Aborted); the dead "this will be done later" placeholder is gone; `Purge-Jobs` is a real DELETE of terminal jobs (`PurgeTerminalJobs`). A time-based retention sweep is deferred to the global TTL-sweep item.**

**TLS:**
- [x] `AuthenticateAsServer` does synchronous, timeout-less network reads on a pool thread - a silent half-open peer (≥1 byte then silence) pins a thread indefinitely (`Proxy/Security/SslStream.cs:200`). **Fixed - the Listener sets `networkStream.ReadTimeout = 15000` before `AuthenticateAsServer` (resetting to infinite after), so a stalled handshake read throws `IOException` after 15s -> the new outer catch/finally logs and disposes everything instead of pinning the thread forever.**
- [x] PEM helpers leak a native BIO per call - incl. one holding the domain private key, on every HTTPS connection via `FromPEM`+`FromPEMPrivateKey` at `Network/Listener.cs:152-153` (`Proxy/Security/X509Certificate.cs:143`). **Fixed - `using var` on every `BasicInputOutput` in `X509Certificate.FromPEM`/`ToPEM` and `Rsa.FromPEMPrivateKey`/`PEMPrivateKey`/`PEMPublicKey`, matching the correct `CryptoKey.FromRSAPrivateKey` pattern. Verified via 100× PEM round-trip.**

**Streaming:**
- [x] DESCRIBE-only MMSH requests spawn live sessions that are never cleaned up (permanent ffmpeg + upstream connection until that station is later played-and-stopped) (`Processors/LocalServer/Streaming/RadioMmshStreaming.cs:1263`). Arm the 30s idle cleanup at session creation, cancel on first `AddClient`. **Fixed - `GetOrCreateSessionAsync` now calls `newSession.ScheduleCleanup(stationId)` right after registering the session, so a session created for a DESCRIBE that never gets a streaming client is reaped after 30s instead of holding ffmpeg + upstream forever. `ScheduleCleanup` was made public and idempotent (cancels any prior timer, no-ops if a client is present or the session is disposed); the first `AddClient` cancels the timer via `_cleanupCts`. Verified the cleanup re-checks client count under the lock before disposing.**
- [x] `CookEncoder.RawFrames` grows without bound for the life of every PNA/RealAudio session (bounded by continuous-playback duration) (`Processors/LocalServer/Streaming/CookEncoder.cs:310`). Gate the collection behind a test-only flag; stop populating it in the streaming pipeline. **Fixed - `RawFrames.Add` is gated behind a new `CollectRawFrames` flag that defaults to false, so the live streaming pipeline no longer accumulates every encoded frame for the session lifetime. The three unit tests that inspect `RawFrames` and the `EncodeWavFileWithEncoder` file/dev helper (used by the `--cook-test` tool) opt in explicitly. Verified: rawframes-off-by-default (count 0) and rawframes-on-when-opted-in (count 100) harness checks pass.**
- [x] `Task.WaitAny` sync-over-async blocks a pool thread per MP3 transcode listener; a non-IOException path orphans ffmpeg (`Processors/LocalServer/Streaming/RadioMp3Streaming.cs:79`). `await Task.WhenAny(...)`, `process.Kill(true)` in a finally (pattern already at `RadioMmshStreaming.cs:1641`). **Fixed - all four transcode sites (Winamp / WMP / Browser / Shoutcast) replace the blocking `Task.WaitAny` with `await Task.WhenAny`, and move `process.Kill(true)` into a `finally` so a non-IOException escaping the copy no longer orphans the ffmpeg process tree.**
- [x] MP3 passthrough branches both call `SetBodyStream` AND stream directly to the socket -> HttpProxy re-sends headers and re-copies the handler-disposed stream (stray header bytes as audio); `HandleShoutcastPlay` leaks its `HttpClient` (`Processors/LocalServer/Streaming/RadioMp3Streaming.cs:106`). Pick one path per branch; add `using`; dispose `Response.Stream` in HttpProxy's catch. **Fixed - the Winamp and Browser MP3 passthrough branches drop the redundant `SetBodyStream` and set `response.Handled = true` before streaming directly, so HttpProxy no longer re-emits headers and re-copies the already-consumed/disposed stream. `HandleShoutcastPlay`'s `httpClient`/`client`/`clientStream` are now `using`-scoped and its MP3 branch streams directly (owning the copy) instead of deferring via `SetBodyStream` to a point after the HttpClient was disposed - fixing the leak. HttpProxy already disposes `httpResponse.Stream` in a `finally` on every path (success/disconnect/fault), so the "dispose in catch" is covered.**
- [x] MMS `OpenFile` issued twice on one connection never decrements the previous live session's client count -> leaked ffmpeg per distinct station id (`Proxy/Mms/MmsSession.cs:206`). `RemoveClient` the previous session at the top of `HandleOpenFile`. **Fixed - `HandleOpenFile` now releases the previous `_liveSession` (`RemoveClient(_stationId)`, then nulls it) at the top, before `_stationId` is reassigned, so a station change on the same connection no longer strands the prior session's client slot (and its ffmpeg/upstream, which the last-client-left cleanup can now reap).**
- [x] MMS `TcpMessageHeader.messageLength` is attacker-controlled and uncapped - a ~1 GB value allocates on the LOH then the follow-up `ReadExactAsync` blocks forever; unauth remote DoS on always-on port 1755 (`Proxy/Mms/MmsCommand.cs:177`). **Fixed - `MAX_COMMAND_BODY_SIZE` (64 KB) cap in `ReadMessageAsync`; oversize lengths return `(false, null)`, the existing disconnect path both callers already handle. Verified via harness: 1 GB declared length drops immediately with no allocation stall, small commands still round-trip.**
- [x] `HandleWmpMp3Stream` ships `Console.Error` `[WMP]` debug scaffolding (same spam class just purged) and a catch-all that eats every failure -> silent empty response, invisible error (`Processors/LocalServer/Streaming/RadioMp3Streaming.cs:195`). `Log.WriteLine`/`WriteException`; return 404/500 on pre-header failure. **Fixed - stripped all eight `Console.Error.WriteLine($"[WMP]...")`/`Flush()` scaffolding lines. The catch-all now `Log.WriteException`s, and a `headersSent` flag lets a pre-header failure (station lookup / upstream connect) return a real `502 Bad Gateway` body instead of a silent empty 200; once bytes are on the wire it logs and stops. Also applied the item-155 fix (await `Task.WhenAny` + `Kill(true)` in `finally`) to this handler's ffmpeg branch.**

**Utilities:**
- [x] `HttpFixerDelegatingStream` sniffs `ICY` on every read for the connection lifetime, ASCII-mangles binary audio, inspects bytes beyond what was read, and can overrun by +5 on the `ICY 200 OK`->`HTTP/1.0 200 OK` expansion (`Utilities/HttpFixerDelegatingStream.cs:15`). First-read-only flag, guard `read>=3`, operate on raw bytes. **Fixed - rewrote it: a `headerChecked` flag limits the sniff to the very first read (every later read is opaque audio returned byte-for-byte); the status-line match is guarded by `read >= 3`; and the rewrite is done as a raw-byte splice (shift the tail right by +5, write the HTTP status in place) instead of an ASCII `GetString`/`Replace`/`GetBytes` round-trip that mangled binary. The +5 expansion only happens when `read + 5 <= buffer.Length`, otherwise the bytes pass through unmodified rather than overrunning the caller's buffer. Verified: rewrite-status / binary-passthrough / non-icy-unchanged / short-read-no-throw / no-overrun-when-tight harness checks pass.**
- [x] `SCUtils` static genre/station Dictionaries mutated from concurrent requests without synchronization on EVERY Shoutcast page request (cache hit or miss) (`Utilities/SCUtils.cs:24`). **Fixed - `genreCache`/`stationCache` are `ConcurrentDictionary` with atomic indexer assignment (replacing the non-atomic `Remove`-then-`Add` pairs).**
- [x] DDG VQD scrape failure persists an empty token with no expiry -> permanently breaks that search term even after DDG recovers; no status check before JSON deserialize (`Utilities/DDGUtils.cs:105`). **Fixed - `VqdSet` only on `Match.Success` + non-empty with status checks on both fetches; the cache-hit check is now `IsNullOrEmpty` so rows already poisoned by the old code self-heal; `Search` soft-fails to an empty result list (renders as "no results") on non-OK status or non-JSON payloads instead of throwing. Verified live: a deliberately poisoned empty row healed to a real VQD on the next lookup.**
- [x] **NEW (found verifying the above):** DDG's links endpoint (`links.duckduckgo.com/d.js`) now answers `202 Accepted` + an `application/x-javascript` anti-bot challenge instead of JSON (observed 2026-07-09), even with a freshly scraped valid VQD - intranet search now soft-fails to zero results rather than 500ing, but it IS effectively dead upstream. Needs a new results source (e.g. `html.duckduckgo.com/html` form-post scrape, `lite.duckduckgo.com`, or a different engine). **Fixed - `DDGUtils.Search` rewritten to POST the no-JS HTML endpoint `html.duckduckgo.com/html/` (`q`/`kl`/`kp`/`s` form fields, standard Firefox UA, no VQD needed) and scrape the server-rendered `result__a`/`result__snippet` anchors. A new pure `ParseHtmlResults` unwraps the `//duckduckgo.com/l/?uddg=` redirect wrapper, strips `<b>` highlight tags, and HTML-decodes titles/snippets. The dead JSON/VQD path (`GetVqd`, `LinksUrl`, `DDGResults` models) is removed. Verified live (query "wikipedia" -> 10 real results with correctly-unwrapped URLs) and with two network-free `DDGTests` covering redirect-unwrap/tag-strip/entity-decode and the empty/garbage cases.**
- [x] NewsUtils Google article decode: sync-over-async `.Result`, per-call undisposed `HttpClient`, no User-Agent, unguarded null `SelectSingleNode` NREs -> 500 on the live article-view path (`Utilities/NewsUtils.cs:139`). **Fixed - `GetDecodingParams`/`DecodeUrls` are now async (no `.Result`), share one static browser-UA `HttpClient` (was a `new HttpClient()` per call), null-guard the `SelectSingleNode` (returns null = fail soft), and `GetGoogleNewsArticle` wraps the whole decode in try/catch returning null on failure instead of 500ing the viewer.**
- [x] `HtmlUtils.NormalizeImages` throws `UriFormatException` on relative/missing `<img>` src from SmartReader output -> 500s the article viewer (`Utilities/HtmlUtils.cs:53`). **Fixed - `Uri.TryCreate(UriKind.Absolute)` + `continue`; nodes without a usable absolute src are left untouched. Verified: a document with src-less, relative, protocol-relative, and absolute imgs normalizes the latter two and skips the rest without throwing.** _(Moved up from the Low list where it also appeared as the `HtmlUtils.cs:53` item.)_

**Build / ops:**
- [x] Curator's uncompressed `articles.json` debug copy lands in `Statics/usenet`, isn't gitignored, and gets embedded into every build (`tools/UsenetCurator/Export/JsonExporter.cs:136`). **Fixed - the debug copy is now opt-in (`CURATOR_DEBUG_JSON=1` env var), added `Statics/usenet/articles.json` to `.gitignore`, and excluded it from the `EmbeddedResource` glob in `VintageHive.csproj`. Only the shipped `.gz` is embedded.**
- [x] `DateNormalizer.ToRfc2822` emits `+00:00` (colon) timezones - RFC-1036/2822-invalid `Date` headers on all 45,410 dated articles in the shipped bundle, up to ~24h inter-article skew (`tools/UsenetCurator/Parsing/DateNormalizer.cs:116`). **Fixed (code) - `ToRfc2822` now formats the zone as numeric `±HHMM` (no colon); zone-less parses already assume UTC. NOTE: the shipped `Statics/usenet/articles.json.gz` still holds the old `+00:00` dates (it stores pre-formatted strings served verbatim by NNTP) - it must be regenerated with a curator run to take effect.**
- [x] Bundled ffmpeg is unusable in Linux/macOS releases: git mode 100644 (not executable) + `FfmpegUtils` resolves it relative to CWD, not the app dir (unlike VFS/Log/DbContext which use `BaseDirectory`) (`Utilities/FfmpegUtils.cs:20`). **Fixed (code) - `FfmpegUtils.GetExecutablePath` resolves `libs/ffmpeg.*` under `AppContext.BaseDirectory`, so a service started from a different CWD finds the bundled binary. NOTE: the exec bit (git mode 100644 -> 100755 via `git update-index --chmod=+x`) is a separate git-attribute change on the committed binaries, not doable from code - left for a maintainer; the Docker image already uses the distro's ffmpeg via PATH.**

### Low - robustness / housekeeping

- [x] (core) `WebSessionGet/Set` bind `@ip`/`@ua` to the session GUID (dead columns); websession rows never expire so admin cookies stay valid forever (`Data/Contexts/HiveDbContext.cs:577`). **Fixed - the meaningless `ip`/`ua` = GUID filters are dropped (key on the cookie only), `WebSessionGet` requires the row within a 7-day TTL, and `WebSessionSet` refreshes `ttl = datetime()` on write so active sessions slide forward and only idle ones expire.**
- [x] (core) `RequestsTrack` wayback prefix strip throws `ArgumentOutOfRange` when the first `_` is the final char, and chops the first char when there's no `_` (`Data/Contexts/HiveDbContext.cs:473`). **Fixed - matches the `_/` wayback marker explicitly via `IndexOf("_/")`; if absent, the URL is left unchanged (no first-char chop, no throw).**
- [x] (socks) Outbound target socket leaks (until GC) when the client write or DB track throws after a successful CONNECT (`Proxy/Socks/Socks5/Socks5Handler.cs:181`). **Fixed - the owning `NetworkStream` using-declaration moved to immediately after the connect try/catch, in BOTH `Socks5Handler` and the identical `Socks4Handler`; the connect-failure catch now also disposes the unconnected socket. Covered by the existing SOCKS suite (all green).**
- [x] (finger) Enumerates the unsynchronized `OscarServer.Sessions` -> `ToList` trailing-null NRE / `GetByScreenName` NRE on null pre-auth `ScreenName` (`Proxy/Finger/FingerServer.cs:72`). **Fixed by the OSCAR `Sessions` change - `FingerServer.BuildUserList` now snapshots `OscarServer.Sessions.Values.ToList()` (safe concurrent enumeration of the `ConcurrentDictionary`), and its `GetByScreenName` lookup uses the null-guarded extension. `BuildUserList` already skipped null/empty `ScreenName`. Existing Finger tests updated for the dictionary and green.**
- [x] (socks) `TunnelAsync` leaves the loser copy pump running (uncaught `ObjectDisposedException`) and lets two readers compete on the client stream after teardown (`Proxy/Socks/SocksProxy.cs:82`). **Fixed - a linked `CancellationTokenSource` is passed to both copy pumps; when either direction finishes, the other is cancelled and both are awaited via `Task.WhenAll`. `CopyStreamAsync` threads the token through Read/Write/Flush and swallows `OperationCanceledException`/`ObjectDisposedException` as expected teardown.**
- [x] (irc) Any LAN client can persist unlimited auto-created channels permanently; `DeleteChannel` is dead code (`Proxy/Irc/IrcProxy.cs:1605`). **Addressed - the "unlimited channels" leak was already bounded in current code: auto-created channels are `IsPersisted=false`, capped at 20/user, and removed when they become empty on both PART and disconnect (`RemoveUserFromAllChannels`). Duplicate ban rows are now skipped via `ConcurrentHashSet.Add` (TryAdd). `IrcDbContext.DeleteChannel` remains an unwired admin method for removing a persisted channel from the DB (not a leak - persisted channels are only the startup `#hive`/`#vintage`); left in place.**
- [x] (telnet) Backspace `TrimEnd` deletes all repeated trailing chars (and batched backspaces do one trim); commands matched without case normalization (`Proxy/Telnet/TelnetServer.cs:60`). **Fixed - backspaces are now applied one char at a time via a `StringBuilder` pass (a run of `\b` removes that many chars; `"aaa\b"` -> `"aa"`, not `""`). `TelnetSession.ProcessCommand` passes the already-lowercased/trimmed `cleanCmd` to `TryAddWindow` instead of the raw command, so mixed-case window names match. Full suite (incl. 14 telnet tests) green.**
- [x] (admin) `/api/iasetyear` missing a `return` after the absent-`year` guard -> `KeyNotFound` 500; also skips the `ValidYears` validation its `HiveController` twin does (`Processors/LocalServer/Controllers/AdminController.cs:217`). **Fixed - early return added, `int.TryParse` + `InternetArchiveProcessor.ValidYears` validation matching the twin; keeps the global `ConfigSet` (the twin's is per-IP) and now stores the parsed int (all readers use `ConfigGet<int>`). Verified by driving the route method: missing/`banana`/`2050` all reply `{"success":false}` without touching config, a valid year persists and replies success.**
- [x] (admin) Static `_brokenUrls` dict grows for the process lifetime (redundant with the persistent 365-day negative cache) (`Processors/LocalServer/Controllers/ApiController.cs:12`). **Fixed - dict deleted; broken URLs now retry after the 365-day cache TTL instead of being pinned until restart. Verified by driving `ImageFetch` against a local dead server: first request fetches once and 404s, the repeat is served from the SQLite negative cache with zero new upstream connections, fallback redirect intact.**
- [x] (admin) Control plane gated only by hardcoded, non-configurable creds; reachable by anyone who can hit the proxy via the loopback-URI rewrite (`http://localhost/` -> admin.hive.com) (`Processors/LocalServer/Controllers/AdminController.cs:359`). **Fixed - the hardcoded `penis`/`penis` check is replaced with config-backed credentials (`ConfigNames.AdminUsername`/`AdminPassword`, default `admin`/`vintagehive`, changeable from the admin panel). NOTE: still a single shared credential over the plain proxy - a per-user DB-backed login with hashed passwords would be the stronger follow-up.**
- [x] (mail) Postmaster thread has no exception handling - a rare `SqliteException` (disk I/O, corruption, external lock) escapes and kills the process (not covered by Listener's catch) (`Proxy/Smtp/SmtpProxy.cs:303`). **Fixed - the postmaster poll loop body is wrapped in try/catch that logs and continues; a transient DB error no longer kills the process.**
- [x] (mail) Postmaster thread runs even when `ServiceSmtp` is off, started in the ctor, never stoppable (`Proxy/Smtp/SmtpProxy.cs:42`). **Fixed - see the "two leaks" item at the top of this audit; startup moved into the gated `ServiceSmtp` path via `StartPostmaster()`.**
- [x] (mail) No dot-stuffing un-stuff on SMTP receive -> IMAP `FETCH` exposes `..` corruption (POP3 masks it); `Replace(EOM)` mangles interior terminators (`Proxy/Smtp/SmtpProxy.cs:190`). **Fixed - the received message now strips ONLY the trailing `\r\n.\r\n` terminator (`mailData[..^EOM.Length]` instead of `Replace(EOM, "")`, which mangled interior sequences) and then reverses dot-stuffing via `UnstuffDots` (leading `..` -> `.`).**
- [x] (netmeeting) The entire H.245 + RTP/RTCP relay wiring is dead code - Q.931 UUIE `H245Address` is never substituted, so clients connect H.245 peer-to-peer and the relay never fires; per call one parked accept task; README overstates it (`Proxy/NetMeeting/H225/H323Server.cs:382`). Rewrite the address or remove the wiring + fix README. **Fixed (remove-the-wiring path) - deleted the `StartH245Relay` invocation and method. It spun up an `H245Handler` listener + background `RunAsync` accept task per Connected call that could never receive a connection: `ProxyDirection` forwards the raw Q.931 payload unchanged, so the peer's real `H245Address` reaches the far side and the two clients negotiate H.245 (hence RTP/RTCP) directly - correct for the LAN case VintageHive targets. Engaging the relay would require rewriting `H245Address` in the forwarded UUIE to point at our listener (a NAT-traversal feature, future work); the unit-tested `H245Handler`/`RtpRelayManager`/`RtpRelay` building blocks are left in place for it. README line corrected to stop claiming an active relay. Removed the two now-unused `using`s; the null-guarded H.245 teardown in `ProxyCallSignaling`'s finally stays (inert, ready if the relay is re-engaged).**
- [x] (netmeeting) `AllocateEvenPort` can produce 65536 -> `ArgumentOutOfRange` escapes the SocketException-only retry, silently failing relay ~1/16384 (`Proxy/NetMeeting/Rtp/RtpRelayManager.cs:261`). **Fixed - odd ports round DOWN (`assigned - 1`) so RTP+1 can never exceed 65535; a rounded-down collision is already covered by `CreateRelay`'s bind-retry. Verified over 300 allocations: all even, all companion ports in range.**
- [x] (netmeeting) Empty `callSignalAddress` SEQUENCE from RRQ -> `IndexOutOfRange` on later admission/call setup; unclamped TTL lets it persist (`Proxy/NetMeeting/H225/RasServer.cs:165`). Reject/guard the `[0]` dereference. **Fixed - `HandleRegistrationRequest` rejects an RRQ with null/empty `CallSignalAddresses` up front (`RRJ_INVALID_CALL_SIGNALING_ADDRESS`) so a zero-address endpoint can never enter the registry. Defence in depth on the read side too: `HandleAdmissionRequest` now treats a destination whose `CallSignalAddresses` is null/empty as not-registered (`ARJ`), and `H323Server` Setup handling guards `calleeEp.CallSignalAddresses[0]` (ReleaseComplete/unreachable) instead of indexing blind. TTL persistence bounded by the `MAX_TTL_SECONDS` clamp from the RAS-registry fix.**
- [x] (oscar) ICQ offline-message sender UIN derived from `string.GetHashCode` - randomized per process run, never matches the live-message sender identity (`Proxy/Oscar/Services/OscarIcqService.cs:601`). Deterministic hash (FNV-1a of lowercased screen name) or a persisted per-user UIN. **Fixed - new `ScreenNameToUin`: a real ICQ sender's screen name IS its numeric UIN, so `uint.TryParse` returns it verbatim; an AIM sender (no UIN) gets a stable synthetic UIN from FNV-1a over the lowercased name. Deterministic across process runs, unlike `string.GetHashCode` (randomized per process since .NET Core), so the offline-message sender now matches the identity the live path presents. Verified: uin numeric-verbatim / case-insensitive / deterministic / distinct-names-differ / null-is-zero harness checks pass.**
- [x] (oscar) Length-prefixed SNAC parsers trust attacker lengths and throw on truncation; the optional user-agent TLV 0x03 null-derefs post-auth and drops sign-on with no error FLAP (`Proxy/Oscar/Services/OscarBuddyListService.cs:172`). Bounds-checked reader; send `AuthFailedError` before closing. **Fixed - `ParseBuddyList` bounds-checks each length byte (`readIdx + 1 + len > data.Length` -> stop) instead of throwing on the range slice for a truncated/hostile list, and reads via `GetString(data, offset, count)`. `ProcessChannelOneAuth` reads the screen-name (0x01) and password (0x02) TLVs defensively and replies `AuthFailedError` when either required TLV is missing (previously a silent NRE + socket drop); the optional user-agent TLV 0x03 is null-coalesced to "unknown" rather than NRE-ing. `AuthFailedError` itself is null-safe on `ScreenName`. Verified: buddylist wellformed / overlong-stops-clean / truncated-tail / empty harness checks pass.**
- [x] (printer) IPP POST responses are raw IPP bytes with no HTTP wrapper - works for the era's WinInet clients, breaks CUPS / strict IPP tools (`Proxy/Printer/PrinterProxy.cs:129`). **Fixed - all IPP POST responses now go through `WrapIpp` -> `HttpResponse.SetBodyData(bytes, "application/ipp")` + `GetResponseEncodedData()` (200 + Content-Length), matching the GET path; the error path returns a proper 500. Note: behavior change on the IPP wire - if an era WinInet client turns out to have depended on the raw-IPP response, this is a one-line revert.**
- [x] (printer) ESC/P conversion failure leaks temp spool files in `printspooler/` (never cleaned); `ProcessEscP` lacks the `GhostScriptNative.IsAvailable` guard `ProcessPostScript` has, so it throws on every job on GS-less installs (`Proxy/Printer/EscPosRenderer.cs:67`). **Fixed - `RenderToPdfAsync` deletes the `.ps`/`.pdf` temp files in a `finally` (even on conversion failure), disposes the page images in an outer `finally`, and guards on `GhostScriptNative.IsAvailable` (returns null with a WARN instead of throwing on GS-less installs).**
- [x] (streaming) Narrow TOCTOU between `GetOrCreateSessionAsync`'s `IsAlive` check and the caller's `AddClient` - the 30s cleanup can dispose the session, ending one connection with no data (self-heals on client retry) (`Processors/LocalServer/Streaming/RadioMmshStreaming.cs:984`). Register the client inside the same lock cleanup checks. **Fixed - `_activeClients`, `_cleanupCts` and the dispose decision are now all serialized under the session `_lock`. `AddClient` takes the lock, returns false if `_disposed` (caller can recreate), otherwise increments and cancels the pending cleanup; the cleanup task re-checks `_activeClients <= 0 && !_disposed` under the same lock and disposes inside it. So a client can no longer register on a session the cleanup is tearing down (and vice-versa) - the increment either happens-before the cleanup's check (no dispose) or after it (AddClient sees disposed).**
- [x] (build) Curator CI/bundle mode has no empty-group guard - a typo'd `--groups` silently overwrites the shipped `Statics/usenet` bundle with `alt.hive.help` only; CI output path is CWD-relative (`tools/UsenetCurator/Setup.cs:21`). **Fixed - `BuildCiConfig` throws if the `--groups` filter matches zero manifest groups, refusing to regenerate the bundle with an empty/near-empty set.**
- [x] (build) Every release artifact ships all three platform ffmpeg binaries (~240 MB/RID) plus foreign-platform OpenSSL/Ghostscript (`VintageHive.csproj:40`). **Fixed (ffmpeg) - the ffmpeg `Content` includes are conditioned on `$(RuntimeIdentifier)`: a no-RID dev build still ships all three (so `dotnet run` works anywhere), but a RID-specific publish ships only that platform's binary (`ffmpeg.osx.intel` covers both osx RIDs). Verified: a `win-x64` publish's `libs/` contains only `ffmpeg.exe`. The OpenSSL/Ghostscript per-RID split is a further optimization left as-is (would need the libs/ inventory mapped per platform).**
- [x] (build) UsenetCurator isn't in `VintageHive.sln`, so CI never compiles it - the tool can silently rot (`tools/UsenetCurator/UsenetCurator.csproj:1`). **Fixed - `UsenetCurator.csproj` added to `VintageHive.sln` (via `dotnet sln add`), so `dotnet build`/CI now compiles it. The full sln builds clean with it included. Moving `DateNormalizer`/parsers under MSTest is a further nicety left for later.**
- [x] (utilities) `HtmlUtils.NormalizeImages` throws `UriFormatException` on relative/missing `<img>` src from SmartReader output -> 500s the article viewer (`Utilities/HtmlUtils.cs:53`). **Fixed - see the Utilities entry in the Medium list above.**
- [x] (utilities) `Extensions.ConfirmValidPath` uses `<= 0` instead of `< 0` on `IndexOfAny` -> a path whose first invalid char is at index 0 is reported valid (no security impact; download branch guarded separately) (`Utilities/Extensions.cs:48`). **Fixed - `< 0`. Verified: `"\0abc"` now reports invalid, interior invalid chars and valid/empty paths unchanged; `ExtensionsTests` all green.**

## New-protocol groundwork (SOCKS5 auth / T.128 app-sharing / Gopher / Yahoo!·MSN)

_Seam map from the audit - reference example is Finger (commit `5bb2471`), the newest end-to-end service. Adding a protocol service touches these 12 places, in wiring order:_

1. **`Proxy/<Name>/<Name>Server.cs`** - subclass `Network.Listener`; ctor `base(addr, port, SocketType.Stream, ProtocolType.Tcp, false)`. Own-the-connection (Finger/SOCKS: do it all in `ProcessConnection`, return `null`, wrap body in try/catch) or request/response (mail/IRC: implement `ProcessRequest`). Call `Mind.Db.RequestsTrack(...)` for dashboard visibility.
2. **`Data/Types/ConfigNames.cs`** - add `Port<Name>` + `Service<Name>` consts (lowercase values).
3. **`Data/Contexts/HiveDbContext.cs` -> `kDefaultGlobalSettings`** - add the port default AND `{ Service<Name>, true }`. Existing installs auto-pick-up via the fallback write-back; no migration.
4. **`Mind.cs`** - three spots: static field, construction in `Init()` reading `ConfigGet<int>(Port<Name>)`, and `StartService(Service<Name>, "<Display>", () => ...Start())` in `Start()`. Toggles apply on restart.
5. **`Processors/LocalServer/Controllers/AdminController.cs` -> `ToggleableServices`** - one `{ "<key>", Service<Name> }` entry feeds both `/api/servicetoggle` and `/api/status`.
6. **`Statics/controllers/admin.hive.com/index.html` -> `serviceLabels`** - add `<key>: "<Display>"`. **Easy to miss (drift trap below).**
7. **`Dockerfile`** - `EXPOSE <port>`. **Currently missed for Finger.**
8. **`README.md`** - four spots: Service Ports row, `### <Name>` feature section, `docker run -p`, and the Service Toggles sentence.
9. **`Tests/VintageHiveTests/<Name>Tests.cs`** - MSTest; extract pure parse/format as `internal static` and write RFC-conformance tests; document intentional deviations. Auto-runs in `ci.yml` - no CI edit.
10. **`todo.md`** - log the integration as checked items under Uncommitted work.
11. *(If it dials out)* respect the processor-chain / DialNine boundary - outbound connects go straight to the live internet by design.
12. *(If it needs per-user auth)* reuse `HiveDbContext.UserFetch` / `UserExistsByUsername` (same store OSCAR/mail/Telnet use) - don't invent a credential table.

**Drift traps (these ARE gotchas):**
- The port registry is copied in **four** places (ConfigNames, kDefaultGlobalSettings, Dockerfile EXPOSE, README) with no single source of truth - Finger already drifted (`EXPOSE 79` was missing; since fixed).
- Admin Services grid **silently drops** any service whose key isn't in BOTH `ToggleableServices` and JS `serviceLabels` (`renderServices` `continue`s) - no error, no warning.
- A missing `kDefaultGlobalSettings` entry fails silent + dangerous: forgotten `Service*` reads `false` (never starts, only INFO log); forgotten `Port*` reads `0` -> `Listener.Run` binds a **random ephemeral port** and nothing throws.
- Port config is **not** universal: OSCAR hardcodes 5190 (incl. in a TLV), MMS/PNA take no port config - copy Finger's ctor, not those. The "core services" block in `Mind.Start()` (HTTP/HTTPS/FTP/Telnet/SOCKS/OSCAR/MMS/PNA) is intentionally non-toggleable - decide which block a new service joins.
- `Listener.Run` is `async void` with floating per-connection `Task.Run` - a new service MUST wrap its handler body in a catch-all (see the fix-first items).

**Per-protocol groundwork that already exists:**
- **SOCKS5 auth**: method-negotiation phase already parses/selects offered methods (`Socks5Handler.cs:19-53`, "NO AUTH only for now"); `Socks5AuthType` enum exists. Work = accept `UsernamePassword (0x02)`, RFC 1929 sub-negotiation, validate via `UserFetch`, add a "require auth" knob. Extend `SocksTests.cs`. (SOCKS4/5 share one port via first-byte dispatch.)
- **T.128 app-sharing**: transport is done - `T120Server` is a working MCS top provider (channel joins, `RelayToChannel`). S20 layer is parse-only (`AppSharing/AppSharingMessage.cs` + `AppSharingConstants.cs`, tests in `AppSharingTests.cs`) and `T120Server` never references it - S20 PDUs are blind-relayed today. Work = S20 session/host logic hooked into the relay path (`Omnet/`/`Chat/`/`Whiteboard/` show the layering). **No new listener/config/toggle** - rides `ServiceT120`/port 1503.
- **Gopher**: nothing exists (only a README link entry). Full 12-step checklist. Closest template is `FingerServer` (one-shot query->response, selector strings are query-shaped); could consume the HTTP processor chain (InternetArchive/ProtoWeb) like `FtpProxy` does if it's meant to proxy.
- **Yahoo!/MSN Messenger**: nothing exists. Template is `Proxy/Oscar/` (server + per-family `Services/`, `oscar_*` tables). Share the presence model - `FingerServer.BuildUserList` reads `OscarServer.Sessions` directly, so a second messenger must feed a common presence abstraction or Finger/cross-messenger presence silently shows only AIM/ICQ. Give it port config from day one (unlike OSCAR's hardcoded 5190).
